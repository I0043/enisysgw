<table class="defaultTable">
  <tr>
    <th><%= t("rumi.config_settings.holiday.table.date") %><%= required('※') %></th>
    <td><%= raw(date_picker7_switch f, 'st_at', errors: @item.errors, mode: :date, value: @item.st_at) %></td>
  </tr>
  <tr>
    <th><%= t("rumi.config_settings.holiday.table.name") %><%= required('※') %></th>
    <td><%= f.text_field("title", style: 'width:30em;') %></td>
  </tr>
</table>
