<%
  new = t("rumi.button.new")
  confirm = t("rumi.ldap_temporary.message.before_create")
%>
<header class="entryHeader">
  <h2><%= @piece_head_title %></h2>
  <span class="navBlock">
    <div class="newEdit">
      <%= link_to image_tag("/images/icon/add.png", alt: new) + " #{new}", system_ldap_temporaries_path(do: "create"), data: {confirm: confirm} %>
    </div>
  </span>
</header>
